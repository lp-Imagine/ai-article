/**
 * AI 模块的纯常量（无副作用、与运行时配置无关）。
 * 把它们从 ai.ts 拆出来，便于阅读与复用。
 */

/** 正文生成时单次章节的最大重试次数 */
export const SECTION_ATTEMPTS = 3;

/** 整理格式时单次请求的纯文本字符上限（避免上游截断） */
export const REFORMAT_CHUNK_PLAIN_CHARS = 1800;

/** 精炼 / 扩写时块间分隔符的正则（由代码维护，模型改写时保留） */
export const TRAILING_HR = /(?:\s*<hr\s*\/?>)+\s*$/i;

/** 大纲生成的角度提示，每个方案走不同切入角避免互相覆盖 */
export const OUTLINE_ANGLES = [
  "问题现场切入：从一个具体的翻车/卡住场景开始，再一步步给出正解",
  "对比选型：在 2-3 个可选做法之间做取舍，给出判断标准与适用边界",
  "最小可用切片：从零搭一个能跑起来的最小版本，再逐步加码",
  "清单式手册：给可直接照做的步骤清单、参数与踩坑边界",
  "认知纠偏：先钉住一个常见误解，用事实和例子逐条拆解",
  "复盘式：按时间线讲一次完整实践，突出关键决策点与代价",
];

